import {
    ReadSync as GetConfVomlSync,
    SaveSync as SetConfVomlSync,
    Read as GetConfVoml,
    Save as SetConfVoml,
    TVoml as TConfVoml,
    TVomlResult as TConfVomlResult
} from './Voml/index'

export {
    GetConfVomlSync,
    SetConfVomlSync,
    GetConfVoml,
    SetConfVoml,
    TConfVoml,
    TConfVomlResult
}